import java.io.File;

public class Runner {
    public static void main(String[] args) {
        RoomBoss roomBoss = new RoomBoss();

        if (args[0] == "<") {
            File file = new File(args[1]);

        } else {

        }
    }
}
